# The private key pins the extension id. Anyone holding it can build an
# extension that impersonates this one, so it never belongs in the repository.
*.pem
*.crx
*.zip
node_modules/
dist/

# Built from src/browser by `npm run build`. Chrome loads extension/, so the
# output lands there, but it is not source.
extension/browser/
extension/protocol.js
